<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="betavision.list">
      <field name="name">betavision list</field>
      <field name="model">betavision.betavision</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="betavision.action_window">
      <field name="name">betavision window</field>
      <field name="res_model">betavision.betavision</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="betavision.action_server">
      <field name="name">betavision server</field>
      <field name="model_id" ref="model_betavision_betavision"/>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="betavision" id="betavision.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="betavision.menu_1" parent="betavision.menu_root"/>
    <menuitem name="Menu 2" id="betavision.menu_2" parent="betavision.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="betavision.menu_1_list" parent="betavision.menu_1"
              action="betavision.action_window"/>
    <menuitem name="Server to list" id="betavision" parent="betavision.menu_2"
              action="betavision.action_server"/>
    -->
  </data>
</odoo>